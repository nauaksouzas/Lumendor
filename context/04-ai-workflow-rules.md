# AI Workflow Rules

1. Plan before code modifications.
2. Maintain progress tracking in `context/05-progress-tracker.md`.
3. Verify changes with read-only tools and automated quality commands (`typecheck`, `test`, `build`).
4. Keep commits clean and focused.
